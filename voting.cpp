#include<stdio.h>
#include<conio.h>
int main(void)
{
	int num,limit;
    printf("enter the number");
		scanf("%D, &num");
	
		printf("enter the number of rows to find:");
		scanf("%d,&limit");
		printf("multiplication table is :");
		for (int i=1; i<=limit; i++)
		{
			getch();
			printf("%d*%d=%d,num,num");
		}

}